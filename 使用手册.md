# XMAgent 使用与维护手册

本文档面向部署、配置和维护 XMAgent 的管理员。示例默认项目目录为
`/opt/XMAgent`、虚拟环境为 `/opt/XMAgent/.venv`、数据目录为
`/opt/XMAgent/data`，实际部署时请替换为你的路径。

XMAgent 是运行在本机或局域网中的 Agent 网关。它把多个微信 iLink 账号、Telegram
Bot 账号和 Telegram 群组路由到独立的 Agent 工作区，并以 SQLite 保存配置、历史、
记忆、消息收发队列和任务。

## 目录导航

- [一、部署前提](#一部署前提)
- [二、安装与启动](#二安装与启动)
- [三、首次初始化与 WebUI](#三首次初始化与-webui)
- [四、渠道配置](#四渠道配置)
- [五、API profile 与模型](#五api-profile-与模型)
- [六、Agent 设置](#六agent-设置)
- [七、聊天命令与流式事件](#七聊天命令与流式事件)
- [八、MCP](#八mcp)
- [九、插件](#九插件)
- [十、记忆](#十记忆)
- [十一、知识库](#十一知识库)
- [十二、文件收发](#十二文件收发)
- [十三、定时任务](#十三定时任务)
- [十四、WebUI/API 参考](#十四webuiapi-参考)
- [十五、运行与数据说明](#十五运行与数据说明)
- [十六、备份、升级与清理](#十六备份升级与清理)
- [十七、故障排查](#十七故障排查)
- [十八、真实网络验证清单](#十八真实网络验证清单)

## 一、部署前提

### 1. 软件和网络

- Python 3.11 或更高版本。
- Linux 或 macOS。Agent 子进程通过 Unix domain socket 调用受控服务能力，
  因此不要把它部署在不支持 Unix socket 的环境中。
- 如果使用 Anthropic，需要能访问 Claude API 或你的 Anthropic-compatible 网关，
  并安装 `claude-agent-sdk==0.2.135`。
- 如果使用 Telegram，需要 Bot Token；如果网络不能直连 Telegram，需要一个
  可用的 HTTP proxy。
- 如果使用微信，需要 iLink 服务可达，真实二维码登录只能在目标环境验证。

### 2. 部署方式

默认服务是 HTTP。开发和内网可直接使用 `serve --host both`；需要公开访问时，使用
`--host 127.0.0.1` 并通过 Nginx、Caddy 或其他 HTTPS 反向代理转发即可。默认
`bypassPermissions` 适合自己管理的 Agent；需要更保守的行为时，可在 Agent 设置中
切换 permission mode。

## 二、安装与启动

### 1. 安装源码

```bash
git clone https://github.com/XMWML/XMAgent.git /opt/XMAgent
cd /opt/XMAgent
python3 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/pip install -e '.[dev]'
```

生产环境不需要开发依赖时可以使用：

```bash
.venv/bin/pip install -e .
```

项目没有把真实配置、数据库或 Bot 凭据放入源码。默认数据目录是当前项目下的
`data/`；可通过命令行全局选项或环境变量覆盖：

```bash
.venv/bin/xmagents --data-dir /srv/xmagents-data doctor
XMAGENTS_DATA_DIR=/srv/xmagents-data .venv/bin/xmagents doctor
```

命令行的 `--data-dir` 放在子命令之前。systemd 中建议使用绝对路径和固定工作目录，
不要依赖 shell 的当前目录。

### 2. 初始化管理员密码

```bash
cd /opt/XMAgent
.venv/bin/xmagents init
```

命令会交互输入两次密码，至少 10 个字符。密码只以 scrypt 哈希写入 SQLite，
不会写入日志。若数据库中已经存在管理员，命令不会覆盖密码；登录 WebUI 后可在
页头修改密码。修改密码会撤销所有既有管理会话，并为当前浏览器建立新会话。

### 3. 前台启动

首次建议仅监听本机：

```bash
.venv/bin/xmagents serve --host 127.0.0.1 --port 8765 --log-level info
```

可信 LAN 且需要 IPv4/IPv6 双栈时：

```bash
.venv/bin/xmagents serve --host both --port 8765
```

`XMAGENTS_HOST`、`XMAGENTS_PORT` 可以提供默认监听地址和端口：

```bash
XMAGENTS_HOST=127.0.0.1 XMAGENTS_PORT=8765 .venv/bin/xmagents serve
```

浏览器访问 `http://127.0.0.1:8765/`。服务启动时会创建持久化 Outbox worker、
Scheduler、Agent 控制 socket，并恢复数据库中状态为 `running` 的渠道。

### 4. systemd 示例

将下面内容保存为 `/etc/systemd/system/xmagents.service`，按部署用户和路径修改：

```ini
[Unit]
Description=XMAgent local multi-channel Agent service
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=xmagent
Group=xmagent
WorkingDirectory=/opt/XMAgent
ExecStart=/opt/XMAgent/.venv/bin/xmagents serve --host 127.0.0.1 --port 8765 --log-level info
Restart=on-failure
RestartSec=5
UMask=0077
Environment=PYTHONUNBUFFERED=1
# 若使用“本机 Claude Code”，PATH 必须能找到该服务用户安装的 claude。
# 请按实际安装位置修改，例如 /home/xmagent/.local/bin 或 /usr/local/bin。
Environment=PATH=/home/xmagent/.local/bin:/usr/local/bin:/usr/bin:/bin
# 也可以把 XMAGENTS_DATA_DIR、ANTHROPIC_BASE_URL 等放在 EnvironmentFile
# 中，但不要把文件提交到 Git。

[Install]
WantedBy=multi-user.target
```

启用和查看：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now xmagents
sudo systemctl status xmagents
sudo journalctl -u xmagents -f
```

服务升级或备份前先停止它，确保 SQLite WAL 中没有仍在写入的内容：

```bash
sudo systemctl stop xmagents
```

## 三、首次初始化与 WebUI

### 1. 登录流程

首次打开根路径会显示初始化表单；如果已经执行过 `xmagents init`，则显示登录表单。
浏览器使用 HttpOnly `xmagents_session` Cookie 保存 14 天会话，并使用独立的
`xmagents_csrf` Cookie 和表单/请求头保护状态改变请求。不要把管理 Cookie 复制给
其他人或嵌入第三方页面。

登录后页头提供“修改密码”和退出。管理 API 的浏览器请求由页面 JavaScript 自动带上
`X-XMAgent-CSRF` 请求头；聊天流式 SSE 通过 `csrf` 查询参数完成同样校验。

### 2. 页面说明

| 页面 | 用途 |
| --- | --- |
| 仪表盘 | Agent、渠道、累计收到消息、最近入站消息、待绑定渠道用户、任务和待投递数量概览 |
| Agent | 创建/编辑 Agent、打开本地对话、查看状态、删除 Agent |
| 渠道与扫码 | 添加 Telegram、启动/停止渠道、二维码登录微信，以及联系人/群组到 Agent 的路由表 |
| 待绑定渠道用户 | 列出尚未绑定 Agent 的微信和 Telegram 私聊；可新建独立 Agent 或绑定已有 Agent |
| API 与模型 | 保存 Anthropic/OpenAI-compatible API profile 和模型列表 |
| MCP 服务 | 保存每 Agent 的 stdio/SSE/HTTP MCP 配置、启停和重连 |
| 插件 | 全局和每 Agent 插件开关 |
| 记忆 | 查看、添加、编辑、启用/禁用和删除 Agent 记忆 |
| 知识库 | 导入、启用/禁用和删除 SQLite 知识库 |
| 定时任务 | 创建和取消 Agent 唤醒任务 |
| 历史与 Outbox | 查看对话消息、发送队列、重试/死信/未知状态 |
| 运行日志 | 查看脱敏审计日志 |

凭据字段在编辑时留空表示保留原值。API 返回和页面只显示是否已配置或掩码，
不会返回完整 Token、API Key、proxy 或 MCP header。

### 3. 常用 API 路径

以下路径都需要管理员会话；浏览器状态改变请求还需要 CSRF token：

```text
GET    /api/health
GET    /api/dashboard
GET    /api/agents
POST   /api/agents
PATCH  /api/agents/{agent_id}
DELETE /api/agents/{agent_id}
GET    /api/agents/{agent_id}/history
GET    /api/agents/{agent_id}/status
POST   /api/agents/{agent_id}/chat
GET    /api/agents/{agent_id}/chat/stream
GET/POST/PATCH/DELETE /api/channels[/{account_id}]
POST   /api/channels/{account_id}/start
POST   /api/channels/{account_id}/stop
POST   /api/channels/wechat/qr/start
GET    /api/channels/wechat/qr/{login_id}
POST   /api/channels/wechat/qr/{login_id}/verify
GET    /api/pending
POST   /api/pending/{peer_id}/approve
GET    /api/bindings
POST   /api/bindings/{peer_id}
DELETE /api/bindings/{peer_id}
GET/POST/PATCH/DELETE /api/apis[/{profile_id}]
GET/POST/PATCH/DELETE /api/mcp[/{server_id}]
GET    /api/agents/{agent_id}/mcp/status
POST   /api/agents/{agent_id}/mcp/{name}/reconnect
POST   /api/agents/{agent_id}/mcp/{name}/toggle
GET    /api/plugins
PATCH  /api/plugins/{command}
PATCH  /api/agents/{agent_id}/plugins/{command}
GET/POST /api/memory/{agent_id}
PATCH/DELETE /api/memory/{agent_id}/{memory_id}
GET    /api/knowledge
POST   /api/knowledge/import
PATCH/DELETE /api/knowledge/{knowledge_id}
GET/POST /api/schedules
DELETE /api/schedules/{schedule_id}
GET    /api/logs
GET    /api/outbox
GET    /api/events
```

`/api/pending` 为兼容路径，返回所有尚未绑定 Agent 的渠道用户，并不只针对 Telegram。

`/api/agents/{agent_id}/chat/stream` 返回 Server-Sent Events，每条数据是脱敏后的
`{kind, content, metadata}`。Anthropic 的部分文本、最终文本和工具启动状态都会
被转成稳定事件；OpenAI-compatible provider 返回文本增量和可能的原始 tool-call
增量，但 XMAgent 不执行 OpenAI function calling。

## 四、渠道配置

### 0. 私聊待绑定流程

微信联系人和 Telegram 私聊使用同一套绑定流程。某个渠道用户第一次发送消息时，服务会
建立该渠道用户记录，但不会自动创建 Agent 或工作区，并立即回复：

```text
未绑定agent，请联系管理员在管理台绑定agent后继续。
```

管理员在 WebUI 的“待绑定渠道用户”页面中可以为该用户新建独立 Agent，或选择已有
Agent 绑定。也可以在“渠道与扫码”的“消息路由”表中查看、改绑或解绑既有路由。完成
绑定后，该用户下一条消息才会进入 Agent 的串行 mailbox；未绑定时收到的附件不会下载到
Agent 工作区。Telegram 白名单群组不走此流程，仍按每个群组一个共享 Agent 处理。

### 1. 微信 iLink

1. 登录 WebUI，进入“渠道与扫码”，点击“扫码添加微信”。
2. 用目标微信扫描页面二维码并按服务返回的状态操作。
3. 如果状态为 `need_verifycode`，在页面输入配对/短信码并提交。
4. `confirmed` 后服务将 Token 和返回的 base URL 保存为一个新的微信渠道并尝试启动。
5. 若自动启动失败，保留渠道记录，在渠道列表中检查地址和状态后重新启动。

二维码状态可能为 `waiting`、`scanned`、`need_verifycode`、`confirmed`、`expired`
或 `error`。同一个二维码会话的重复轮询会返回同一个确认结果，因此一次扫码确认只会
创建一个微信渠道，不会因为浏览器并发请求重复添加。渠道凭据仍在数据库中。

当前适配器按 iLink 一对一联系人设计，不提供微信群路由。微信联系人首条消息会进入
上面的“待绑定渠道用户”流程；管理员绑定后，该联系人后续消息进入同一个串行 mailbox。
附件会在 Agent 已绑定后的入站消息中下载到其 `uploads/` 子目录。

微信渠道 JSON 配置示例：

```json
{
  "min_send_interval": 1.5,
  "max_text_length": 2000
}
```

`min_send_interval` 是同一微信账号的发送最小间隔，默认约 1.5 秒；`max_text_length`
控制一块文本的最大长度，默认 2000。服务还会通过持久化 Outbox 按段落/句子合并和
限频发送。微信 iLink 回复依赖入站 context token，定时任务结果没有新 token 时会
暂存，等该联系人下一条消息到达后再发送。

### 2. Telegram Bot

1. 在 BotFather 创建 Bot，复制 Bot Token。
2. WebUI 进入“渠道与扫码”，点击“添加 Telegram”。
3. 填写名称、Token；需要代理时填写 `http://host:port` 或兼容的 HTTP proxy URL。
4. 保存后点击“启动”。服务会先调用 `getMe` 验证 Token，再启动 `getUpdates` 长轮询。

Telegram 渠道 JSON 配置示例：

```json
{
  "poll_timeout": 25,
  "group_allowlist": ["-1001234567890"]
}
```

每个 Bot 有独立 HTTP 客户端，默认不继承宿主机环境代理；只有填写的 `proxy` 会用于
该 Bot。`poll_timeout` 是 `getUpdates` 的长轮询秒数。Telegram update offset 会在
每条成功处理的消息后持久化，避免同一批中后续消息因前一条失败而被跳过。

Telegram 私聊首条消息同样进入上面的“待绑定渠道用户”流程，并收到固定的未绑定提示。
管理员可在该页面新建独立 Agent 或绑定现有 Agent；渠道页的“消息路由”表列出每个
联系人或群组当前使用的 Agent，也可随时改绑。未加入 `group_allowlist` 的群组不会进入
Agent。已加入白名单的群组
每群共享一个 Agent，仅处理：

- `/status` 等命令；
- `@Bot` 提及的消息；
- 回复 Bot 的消息。

群组中的 `/status@bot_username` 会在送入通用命令解析器前规范化为 `/status`；发给
其他 Bot 的命令不会触发当前 Bot。

Telegram 文本会按 4096 字符上限分块。模型产生文本增量时，服务默认每约 0.7 秒
调用 `editMessageText` 更新第一条消息，最终结果再完成分块发送。网络或 Telegram
返回 429/5xx 时由 Outbox 按退避重试；超时等无法确认结果的错误标记为 `unknown`，
不会自动盲目重复发送。

### 3. 渠道生命周期

渠道列表中的 `running` 状态会在服务重启时自动恢复；`stopped` 不会自动连接。
凭据、proxy 或 JSON 配置更新时，正在运行的适配器会尝试重载。遇到网络错误时，
先点击“停止”，确认状态变化，再点击“启动”。删除渠道会同时删除该账号下的
remote peers、绑定和渠道级收发数据，请先导出需要保留的历史。

## 五、API profile 与模型

### 1. 本机 Claude Code

“API 与模型”页面会自动提供不可编辑的 **本机 Claude Code** profile。它不需要在
XMAgent 中填写 API Key 或 API URL，而是复用运行服务的系统用户已有 Claude Code
认证上下文。XMAgent 会优先调用该服务用户 PATH 中安装的 `claude` 可执行文件；未找到时才由 SDK 使用其随附的后备程序。systemd 部署时应在服务文件中明确设置包含 `claude` 的 `PATH`；也可在 Agent 高级 JSON 的 `claude_options.cli_path` 中填写绝对路径。

可使用该服务用户预先完成的 Claude Code 登录：

```bash
claude login
claude auth status
```

例如 systemd 使用 `User=xmagent` 时，应切换到 `xmagent` 用户完成登录。此 profile 也会
保留服务进程启动时继承的 `ANTHROPIC_AUTH_TOKEN` 和 `ANTHROPIC_BASE_URL`，因此可以
使用系统服务环境或 `EnvironmentFile` 提供的 API/计费凭据与兼容网关配置。它不会清除、
重写或隔离这两个环境变量；同时不会用工作区专用配置替换服务用户已有的 Claude Code
配置目录。

这两种认证来源可分别使用：已有 Claude Code 登录时无需在 profile 中重复填写凭据；
使用 API/计费环境变量时，由启动 XMAgent 的服务进程继承它们即可。若希望由 XMAgent
保存某一套独立凭据和模型列表，请创建下面的普通 Anthropic profile。

### 2. Anthropic

在“API 与模型”中新增 profile：

- `provider`: `anthropic`
- `base_url`: 官方 API 或兼容网关地址，可留空使用环境默认值
- `API Key`: 写入 `ANTHROPIC_AUTH_TOKEN`
- `models`: 每行一个模型名，例如 `claude-sonnet-4-20250514`
- `options`: provider 额外 JSON（会与 Agent 配置合并）

Agent 关联该 profile 后，SDK options 会注入：

```text
ANTHROPIC_AUTH_TOKEN=<profile secret 或进程环境变量>
ANTHROPIC_BASE_URL=<profile base_url 或进程环境变量>
```

也可以在启动服务前设置：

```bash
export ANTHROPIC_AUTH_TOKEN='***'
export ANTHROPIC_BASE_URL='https://your-gateway.example/v1'
```

环境变量不会写入数据库，但会传给 Anthropic SDK 子进程。API profile 中的 secret
会写入本机 SQLite，因此应同时保护环境文件和 `data/`。

### 3. OpenAI-compatible

设置 `provider` 为 `openai`。服务调用 Chat Completions SSE：

```text
POST <base_url>/v1/chat/completions
Authorization: Bearer <API Key>
```

如果 `base_url` 已经以 `/chat/completions` 结尾则直接使用；否则服务会补齐 `/v1`
和 `/chat/completions`。兼容网关不是所有都需要 `/v1`，遇到 404 时应把完整接口地址
填写为以 `/chat/completions` 结尾的 URL。

OpenAI provider 支持文本、多轮应用层历史和 SSE 增量；它不承诺 Claude Code 工具、
MCP 或 Anthropic `WebSearch`/`WebFetch` 等价能力。`options` 中的 OpenAI 参数放在
Agent 配置的 `openai_options` 下，例如：

```json
{
  "openai_options": {
    "temperature": 0.2,
    "max_tokens": 4096
  }
}
```

### 4. 多模型

一个 profile 的 `models` 可以保存多个模型名；Agent 的 `model` 字段决定本次使用的
具体名称。模型列表主要用于管理台记录和人工选择，服务不会自动在列表之间轮换。
`fallback_model` 需要通过 Agent 的 `claude_options` 传给 SDK；OpenAI provider 不会
自动切换 fallback。

## 六、Agent 设置

每个 Agent 都有独立的：

- SQLite Agent 记录和持久化对话历史；
- Claude SDK 会话状态（运行时重建后按历史恢复近期上下文）；
- `data/workspaces/<Agent 名称>/` 工作区；
- `CLAUDE.md` 和记忆条目；
- 插件开关、知识库关联和渠道绑定。

### 1. 基础字段

| 字段 | 说明 |
| --- | --- |
| Provider | `anthropic` 或 `openai` |
| API profile | 选择已保存的 API profile |
| Model | 具体模型名，可留空使用 provider 默认值 |
| Permission | `bypassPermissions`、`default`、`acceptEdits`、`plan`、`dontAsk`、`auto` |
| Effort | `low`、`medium`、`high`、`xhigh`、`max` |
| System prompt | Anthropic 会把它与 XMAgent 工作区规则拼接；OpenAI-compatible provider 只使用该自定义提示 |
| Memory | 是否把此 Agent 的记忆注入上下文 |
| Knowledge base | 选择一个已导入的知识库；留空表示禁用 |
| 最大上下文消息数 | 可选，按消息条数保留最近内容 |
| 最大上下文 token | 可选，按约每 4 个字符一个 token 的本地估算裁剪 |

不设置上下文上限时，服务会保留应用层历史；SDK 仍可能受模型自身上下文窗口限制。
设置上限后会保留近期消息，必要时重建 Claude session，使裁剪生效。系统提示、记忆
和自动知识结果仍会在新请求中重新注入。

### 2. Claude SDK JSON

“Claude SDK 高级 JSON”对应可序列化的 `ClaudeAgentOptions` 字段。常用示例：

```json
{
  "claude_options": {
    "tools": ["Read", "Bash"],
    "allowed_tools": ["Read"],
    "disallowed_tools": ["Bash(rm *)"],
    "max_turns": 8,
    "max_budget_usd": 0.5,
    "fallback_model": "claude-haiku-3-5-20241022",
    "thinking": {"type": "enabled", "budget_tokens": 12000},
    "setting_sources": ["project"],
    "include_hook_events": true
  }
}
```

当前 SDK 支持的完整字段以实际安装版本 `claude-agent-sdk==0.2.135` 的
`ClaudeAgentOptions` 签名和[官方 SDK 文档](https://docs.anthropic.com/en/docs/claude-code/sdk/sdk-python)
为准；升级 SDK 后应重新运行测试并检查高级配置字段。
服务会通过 SDK 构造函数签名过滤未知字段，因此保存新版本字段后在旧 SDK 上会被忽略。
MCP 存在时服务默认加入 `strict_mcp_config=true`，避免意外加载其他配置来源。
当前 WebUI 的常用表单会写入 `system_prompt`、上下文上限、WebSearch/WebFetch 和
`claude_options`；其他可序列化字段请放在 `claude_options` JSON 中。`env` 可配置普通
环境变量，服务会保留必要的 Agent 控制变量，并把 API 认证变量注入普通 Anthropic 子进程。

Agent 名称全局唯一，并直接作为工作目录名。重命名 Agent 时，服务会连同文件把工作目录
迁移到新名称；名称不能包含 `/` 或 `\`。普通 Anthropic profile 使用工作区内独立的
`.claude-config/` 作为 `CLAUDE_CONFIG_DIR`；本机 Claude Code profile 不替换服务用户
已有的 Claude Code 配置，服务进程继承的 `ANTHROPIC_AUTH_TOKEN` 与
`ANTHROPIC_BASE_URL` 也会保持可用。

### 3. 高级 Python build_options

只有管理员可以编辑。保存时先做语法检查，运行时在隔离的 Python namespace 中提供
`ClaudeAgentOptions` 和 `sdk`，可定义：

```python
def build_options(options):
    options["max_turns"] = 12
    options.setdefault("allowed_tools", []).append("Read")
    return options
```

函数也可以原地修改后返回 `None`，或者返回 `ClaudeAgentOptions` 实例。运行错误会
回退到基础配置，并在审计日志写入失败类型。该选项用于管理员的高级定制。

## 七、聊天命令与流式事件

### 1. 系统命令

| 命令 | 行为 |
| --- | --- |
| `/new` | 中断当前任务，清除当前渠道用户的上下文并重建会话；保留历史、记忆和文件 |
| `/effort` | 查看当前思考强度 |
| `/effort low\|medium\|high\|xhigh\|max` | 保存强度并重建 Anthropic 会话 |
| `/status` | 显示 Agent、provider、模型、effort、权限、上下文、记忆、知识库、MCP 和队列 |
| `/help` | 自动列出系统命令和当前启用插件 |
| `/sendfile relative/path [caption]` | 通过当前渠道发送工作区内文件 |

`/new` 的作用范围是当前 Agent + 当前 peer 路由；一个 Agent 绑定多个 peer 时，
不同用户不会共享 OpenAI 应用层历史。`/effort` 不接受任意字符串，非法值只返回用法。

### 2. 工具请求

Anthropic SDK 的工具启动会转为脱敏的工具事件，只显示工具名和经过截断/脱敏的输入
预览，不把 Token、密码、context token 等敏感字段转发到聊天渠道。Telegram 会在
最终文本前发送简短“正在使用工具”提示；微信同样通过 Outbox 发送，但不提供高频
实时文本流。

WebUI Agent 对话使用 `/api/agents/{id}/chat/stream` 的 SSE。浏览器会实时显示文本增量、
最终文本和工具状态；刷新页面不会自动恢复一条已中断的 SSE 请求，完整历史仍可在
“历史与 Outbox”查看。

### 3. OpenAI 与 Anthropic 的差异

Anthropic 使用 `ClaudeSDKClient` 的持续 session，可接 SDK 工具和 MCP。OpenAI provider
每轮将当前 peer 的应用层历史重新发送给 Chat Completions，支持文本 SSE，但不会执行
Claude SDK 工具调用。`/status` 会显示当前 provider 和模型，便于确认实际路径。

## 八、MCP

### 1. 添加配置

进入“MCP 服务”，选择 Agent，填写名称和 JSON。支持 SDK 文档中的三种外部传输：

stdio：

```json
{
  "type": "stdio",
  "command": "/opt/tools/my-mcp",
  "args": ["--mode", "stdio"],
  "env": {"MCP_MODE": "readonly"}
}
```

SSE：

```json
{
  "type": "sse",
  "url": "https://mcp.example/sse",
  "headers": {"Authorization": "Bearer ..."}
}
```

HTTP：

```json
{
  "type": "http",
  "url": "https://mcp.example/mcp",
  "headers": {"X-API-Key": "..."}
}
```

Headers、环境变量和命令行参数属于管理员配置，可能包含凭据。页面展示会脱敏，
不要把配置 JSON 粘贴到公开 issue 或日志。

### 2. 启停、重连与状态

保存或修改 MCP 后，服务会使该 Agent runtime 失效，并在下一次聊天时用新配置建立
会话。若当前 SDK session 已连接，页面可以调用“启用/停用”和“重连”；具体能力取决于
安装版本是否提供 `toggle_mcp_server`、`reconnect_mcp_server`。进入 Agent 状态或
MCP 页面查看 `connected`、`mcpServers` 和错误信息。

### 3. 知识库 MCP

Anthropic Agent 关联知识库后，服务会额外创建只读的内置 MCP server，提供
`knowledge_schema` 和 `knowledge_search`。它只读取当前 Agent 关联的 `documents`
数据，不执行 SQL 写入。OpenAI Agent 没有该工具，但仍会得到应用层自动关键词检索结果。

## 九、插件

### 1. 文件规范

每个插件必须放在 `plugins/<name>/<name>.py`，目录名和 Python 文件名保持一致。模块
至少提供 `PLUGIN` 元数据和 `handle(ctx, args)`：

```python
PLUGIN = {
    "name": "Test",
    "command": "test",
    "description": "回显输入文本",
    "usage": "/test text1",
}

async def handle(ctx, args: str) -> str:
    return f"输入了{args}"
```

`handle` 可以是同步或异步函数。`ctx` 提供：

```text
ctx.agent        当前 AgentRuntime
ctx.settings     AgentSettings
ctx.channel      webui / wechat / telegram / schedule
ctx.user_id      当前发送者
ctx.peer_id      当前渠道 peer
ctx.workspace    当前工作区 Path
ctx.metadata     渠道元数据
await ctx.reply(text)
await ctx.file(relative_path, caption=...)
```

`ctx.file()` 和 `/sendfile` 一样只应使用工作区相对路径，服务会再次做真实路径校验。
插件抛出的异常会被转成插件错误回复，不会关闭 Agent mailbox。

### 2. 示例插件

项目内置：

- `/ipv6`：返回通过本机 DNS/网卡枚举发现的非回环 IPv6 地址；未发现时返回提示。
- `/test text1`：返回 `输入了text1`。

### 3. 开关和加载

WebUI 的全局开关控制所有 Agent；每个 Agent 还有独立开关。插件状态写入
`plugins` 和 `agent_plugins` 表。新建或修改插件文件后需要重启服务，因为插件在
`AppService` 创建时扫描加载；重启后会恢复数据库中的全局和 Agent 开关。

## 十、记忆

### 1. 默认策略

记忆默认开启，但采用保守的本地规则，不额外调用模型自动总结：

- 用户发送 `记住：我的项目是 XMAgent` 或 `remember: ...` 时保存事实；
- 简短的“我叫……”“我的时区是……”“我住在……”等稳定声明可被保存；
- 普通闲聊、助手猜测和助手输出不会自动变成记忆；
- 每个 Agent 独立保存 `memory_entries`，不同用户不会合并；
- 每轮回复结束后后台提炼，提炼失败不影响主回复。

### 2. CLAUDE.md

启用的记忆会同步到工作区 `CLAUDE.md` 的标记区域：

```text
<!-- XMAGENT_MEMORY_START -->
## XMAgent persistent memory
...
<!-- XMAGENT_MEMORY_END -->
```

标记区域之外的人工内容会保留。管理员可在“记忆”页查看、添加、编辑、禁用和删除；
删除后服务同步更新该区域。关闭 Agent 的 `memory_enabled` 会停止上下文注入和后台
观察，但不会自动删除既有记录。

### 3. 记忆类型

在“添加记忆”窗口中可选择以下整理标签：

| 类型 | 用途 |
| --- | --- |
| `fact` | 管理员手动补充的一般事实 |
| `explicit` | 用户明确要求“记住”的内容 |
| `summary` | 对较长背景的压缩摘要 |

三种类型在当前版本中都会按启用状态参与上下文；类型用于让管理员更容易筛选和维护。

## 十一、知识库

### 1. SQLite 格式

导入文件必须包含如下表和字段，五个字段全部为 `TEXT`，`id` 必须是主键，`content`
必须 `NOT NULL`：

```sql
CREATE TABLE documents (
  id TEXT PRIMARY KEY,
  title TEXT,
  content TEXT NOT NULL,
  metadata_json TEXT,
  updated_at TEXT
);
```

允许存在其他表/字段，但服务只读取 `documents`。导入接口和文件附件共用 100 MB
上限。服务先以只读方式校验源库，再复制到 `data/knowledge/<id>.sqlite3`，创建
`<id>.fts.sqlite3` 的只读 FTS5 索引；源文件不会被修改。

### 2. 导入和关联

1. 打开“知识库”，点击“导入 SQLite”，输入名称并选择文件。
2. 导入成功后，打开目标 Agent 设置并选择知识库。
3. 可以在知识库页停用/启用；删除会解除所有 Agent 的关联并删除服务自己的副本。

每轮请求先按用户文本做轻量关键词检索，将最多若干条结果注入 `<knowledge>` 区域。
Anthropic 还可以主动调用 `knowledge_schema`/`knowledge_search`；OpenAI 只使用自动
注入，不提供 MCP 工具。当前版本不内置向量 embedding/RAG 服务，若需要语义检索，
应在导入前自行生成可搜索的文本内容或另接 MCP。

### 3. 维护建议

知识库导入是复制操作，更新源数据库后不会自动同步。正确做法是删除旧库、重新导入
新副本，再把 Agent 关联到新 ID。请不要直接修改 `data/knowledge/` 中的服务副本，
否则 FTS 索引可能与内容不一致。

## 十二、文件收发

### 1. 用户发送文件给 Agent

Telegram 或微信收到图片/文件后，服务先下载到当前 Agent 工作区的 `uploads/`，再
在 Agent prompt 追加已校验的本地文件路径。附件模型仍保留文件名、类型、大小和
provider 来源信息，供运行时和历史处理使用；当前 prompt 注入的核心是可读取路径。
微信媒体按账号和用户哈希
分目录，避免不同账号文件名冲突；iLink 媒体会按协议 AES-ECB/PKCS#7 解密。Telegram
通过 `getFile` 下载。

服务不会把未确认属于当前 Agent 工作区的路径注入上下文。附件和知识库上传最大 100 MB，
文件名会清理路径分隔符和控制字符。

### 2. Agent 发送文件

聊天中使用：

```text
/sendfile uploads/report.pdf 报告
```

管理员 shell 可直接指定 Agent：

```bash
.venv/bin/xmagents send-file <agent-id> uploads/report.pdf --caption 报告
```

Agent 工作区内推荐使用受控命令：

```bash
xma-send-file uploads/report.pdf --caption 报告
# 等价入口：xma agent-send-file uploads/report.pdf --caption 报告
```

服务会验证文件真实路径必须位于当前 Agent 工作区，拒绝 `..` 越界、符号链接逃逸、
不存在文件和超过 100 MB 的文件。Outbox 发送前还会再次验证，防止入队后文件被替换成
工作区外的 symlink。

WebUI 没有通用的“从浏览器上传到 Agent 工作区”接口；浏览器上传入口仅用于知识库
SQLite 导入。用户文件应通过微信/Telegram 渠道发送，或由管理员先放入相应 workspace。

## 十三、定时任务

### 1. 表达式

Scheduler 使用 SQLite 持久化、`asyncio` worker 和 `zoneinfo`：

| 类型 | 格式 | 示例 |
| --- | --- | --- |
| `at` | ISO 8601 时间 | `2026-08-13T09:00:00+08:00` |
| `every` | 正数秒数 | `3600` |
| `cron` | 五字段 Cron | `0 9 * * *` |

创建时只接受已解析的明确值，不会把“每天早上九点”再次交给模型解释。Cron 时区
默认 `Asia/Shanghai`，也可以指定任何系统 `zoneinfo` 可识别的时区。

### 2. 管理员 CLI

```bash
# 三种时间方式只能选择一种
.venv/bin/xma schedule create <agent-id> \
  --at 2026-08-13T09:00:00+08:00 \
  --prompt "发送日报"

.venv/bin/xma schedule create <agent-id> \
  --every 3600 --prompt "检查服务状态"

.venv/bin/xma schedule create <agent-id> \
  --cron "0 9 * * *" --timezone Asia/Shanghai \
  --prompt "执行早间检查"

.venv/bin/xma schedule list <agent-id>
.venv/bin/xma schedule cancel <schedule-id>
```

也可以把 prompt 作为位置参数：

```bash
.venv/bin/xma schedule create <agent-id> --every 600 检查队列
```

`--prompt` 和位置参数不能同时使用。`--peer-id` 可把结果回传给已批准并绑定到该
Agent 的渠道用户：

```bash
.venv/bin/xma schedule create <agent-id> --every 86400 \
  --peer-id <remote-peer-id> --prompt "发送每日提醒"
```

### 3. Agent 受控 CLI

Claude SDK 子进程会收到当前 Agent 的临时环境变量：

```text
XMAGENTS_AGENT_ID
XMAGENTS_WORKSPACE
XMAGENTS_CONTROL_SOCKET
XMAGENTS_CONTROL_SECRET
```

完整环境下执行 `xma schedule create|list|cancel` 或 `xma-send-file` 会通过
`data/runtime/control.sock` 请求服务，不能打开管理员 SQLite 或访问其他 Agent。
没有完整 capability 时，命令拒绝执行。服务重启会生成新的 capability，旧环境自动失效。

### 4. 回传和失败

任务运行时使用目标 Agent 的同一串行 mailbox，避免与普通聊天并发修改上下文。Telegram
任务可以主动发送结果；微信 iLink 回复需要新 context token，因此先写入 `deferred`
Outbox，等下一条微信消息到达。任务表保留 `last_error`、`last_run_at` 和 `next_run_at`：

- 一次性 `at` 成功后自动禁用；失败后禁用并保留错误；
- `every`/`cron` 成功后计算下一次时间；
- 周期任务失败后跳过当前到期窗口，在下一次计划时间重试；表达式失效则禁用。

## 十四、WebUI/API 参考

### 1. Agent 创建示例

```json
{
  "name": "研发助手",
  "provider": "anthropic",
  "api_profile_id": "<profile-id>",
  "model": "claude-sonnet-4-20250514",
  "permission_mode": "acceptEdits",
  "effort": "high",
  "memory_enabled": true,
  "knowledge_base_id": "<knowledge-id>",
  "config": {
    "system_prompt": "回答时优先使用中文。",
    "max_context_messages": 40,
    "max_context_tokens": 16000,
    "enable_web_search": false,
    "enable_web_fetch": false,
    "claude_options": {"tools": ["Read"], "max_turns": 8}
  }
}
```

`config` 是 JSON 对象。保存高级 Python 时服务会先 `compile()`；语法错误返回 400，
不会覆盖旧配置。Agent、API profile、MCP 等配置变化会使对应 runtime 失效，下一轮
请求重新构建。

### 2. 管理 API 的秘密处理

`/api/channels`、`/api/apis`、`/api/mcp` 和 `/api/outbox` 的返回值会递归脱敏。不要
根据页面的 `***` 值回写秘密；编辑表单留空即可保留旧值。`api_profiles.enabled` 和
`channel_accounts.status` 会显示在管理台；真正决定 Agent 使用哪组模型的是 Agent
关联的 `api_profile_id`、provider 和 model。

### 3. SSE 客户端注意事项

浏览器原生 `EventSource` 只能方便地发送 GET，因此聊天流的 CSRF token 通过
`/api/agents/{id}/chat/stream?...&csrf=<token>` 传入。自定义客户端应先取得登录 Cookie
和 `xmagents_csrf` Cookie，再把 token 放在 `X-XMAgent-CSRF` header 或 `csrf` 查询参数。
不要把 SSE URL 分享给其他人，因为 URL 可能出现在代理访问日志中。

## 十五、运行与数据说明

### 1. 本地数据

服务启动时尝试将以下目录设为 `0700`：

```text
data/
data/workspaces/
data/knowledge/
data/runtime/
```

SQLite 主库及 `-wal`/`-shm` sidecar、知识库副本、FTS 索引和控制 socket 尝试设为
`0600`。权限检查可用：

```bash
find data -maxdepth 2 -type f -print0 | xargs -0 -n1 stat -c '%a %n'  # Linux
find data -maxdepth 2 -type f -print0 | xargs -0 -n1 stat -f '%Lp %N'  # macOS
```

若文件系统、容器挂载或 umask 不支持这些权限，请在部署用户和挂载选项中调整。

### 2. 凭据和日志

Token、API Key、密码、proxy、MCP headers、context token、AES key 会在管理 API、
工具事件、异常和 Outbox 展示中脱敏。数据库用于保存渠道和模型配置，备份时应与工作区
一起保存；`data/`、`.claude-config/`、`.env` 和完整日志不应提交到 Git。

### 3. 控制 socket

控制 socket 只接受一行 JSON，限制请求/响应大小和处理时间。服务端验证 capability、
Agent 绑定、批准 peer、workspace realpath 和文件大小。它不是通用管理 RPC，不支持
执行任意 SQL 或发送任意账号的文件。

## 十六、备份、升级与清理

### 1. 备份

备份必须包含 SQLite、工作区、知识库和渠道配置；渠道 Token 位于 SQLite，不能只备份
源码。推荐停服后复制整个数据目录，并保留权限：

```bash
sudo systemctl stop xmagents
sudo rsync -a --numeric-ids /opt/XMAgent/data/ /srv/backups/xmagents-$(date +%F)/
sudo systemctl start xmagents
```

不要只复制 `xmagents.sqlite3` 而忽略同时存在的 `-wal`/`-shm`。停服复制可以保证
SQLite WAL 已安全落盘；恢复时把整个备份目录作为 `XMAGENTS_DATA_DIR`，检查权限后
再启动。恢复旧数据会恢复旧管理员会话和渠道状态，必要时应立即修改管理员密码。

### 2. 升级

```bash
sudo systemctl stop xmagents
cd /opt/XMAgent
git fetch --all
git pull --ff-only
.venv/bin/pip install -e '.[dev]'
.venv/bin/python -m pytest -q
.venv/bin/python -m compileall -q xmagents plugins
.venv/bin/xmagents doctor
sudo systemctl start xmagents
```

升级前建议先做数据备份。当前数据库初始化使用 `CREATE TABLE IF NOT EXISTS`，但仍应
把依赖版本和应用版本一起记录；若未来版本发布迁移脚本，应先阅读发行说明再启动。

### 3. 可安全删除的开发生成物

部署到服务器时不要携带本机测试数据。停止服务后可以删除当前源码目录中的：

```text
venv/ 或 .venv/       本机虚拟环境（服务器重新安装）
.pytest_cache/
**/__pycache__/
xmagents.egg-info/
.DS_Store
data/xmagents.sqlite3*   仅当确认不需要本机数据时
data/runtime/control.sock  服务停止后若残留
```

生产机上的 `data/`、工作区、知识库和备份不是“缓存”，不要按大小批量删除。清理前
应逐项确认路径、大小和是否已有备份。

## 十七、故障排查

### 1. `xmagents serve` 提示先初始化

执行：

```bash
.venv/bin/xmagents init
.venv/bin/xmagents doctor
```

确认命令使用的当前目录、`XMAGENTS_DATA_DIR` 和 WebUI 使用的是同一个数据目录。

### 2. 页面能打开但状态改变返回 403

这是 CSRF 保护。返回登录页后重新登录，确认浏览器没有拦截 `xmagents_csrf` Cookie；
自定义 API 客户端需要发送对应的 `X-XMAgent-CSRF` header。不要通过关闭 CSRF 来解决。

### 3. Agent 回复“Anthropic runtime requires...”

当前环境没有安装 Claude SDK 或虚拟环境错误：

```bash
.venv/bin/pip install -e .
.venv/bin/python -c 'import claude_agent_sdk; print(claude_agent_sdk.__file__)'
```

OpenAI Agent 不需要 Claude SDK 的网络会话，但仍需 `httpx` 和可用 API profile。

### 4. Telegram 无消息或一直重连

按顺序检查：

1. Token 是否完整且没有多余空格。
2. WebUI 渠道状态是否为 `running`。
3. `getMe` 是否可通过该 Bot 的 proxy 访问。
4. proxy 是否是 HTTP proxy URL，而不是只支持 SOCKS 的地址。
5. 日志是否出现 401、409、429 或 5xx。
6. 群组是否已加入 `group_allowlist`，消息是否 @Bot/回复 Bot。

429/5xx 会进入 Outbox 重试；网络超时可能为 `unknown`，请在 Outbox 确认后再决定
是否人工重发，避免重复消息。

### 5. 微信二维码过期或自动启动失败

重新点击“扫码添加微信”，不要重复提交旧二维码的配对码。若确认后渠道显示 error，
检查返回的 base URL、iLink 网络和 Token 是否仍有效，然后在渠道页启动。微信定时结果
显示 `deferred` 是设计行为，等待该联系人下一条消息提供新的 context token。

### 6. MCP 显示 failed/needs-auth

检查 JSON 的 `type`、`command`/`url`、args、headers 和 stdio 程序权限。先停用并保存，
确认外部 MCP 可单独启动，再启用并点击重连。若安装的 SDK 不提供动态 toggle/reconnect，
修改后让 Agent 发起新一轮会话即可重建。

### 7. 文件发送失败

确认相对路径相对于当前 Agent workspace，而不是项目根目录。例如文件位于
`data/workspaces/<Agent 名称>/uploads/a.pdf`，命令应使用 `uploads/a.pdf`。检查：

```bash
realpath 'data/workspaces/<Agent 名称>/uploads/a.pdf'
stat 'data/workspaces/<Agent 名称>/uploads/a.pdf'
```

拒绝 `..`、symlink 越界、不存在文件和大于 100 MB 文件。Outbox 中如果是 `unknown`，
不要立即重复发送；先确认渠道端是否已经收到。

### 8. 定时任务没有运行

确认服务实际处于运行状态，任务 `enabled=1`，并检查 WebUI 任务的 `next_run_at`、
`last_error` 和时区。`at` 时间使用带时区的 ISO 8601 最稳妥；Cron 必须正好五字段。
服务停止期间任务不会执行，重新启动后会处理已经到期的持久任务。

### 9. 控制 socket 残留或 Agent CLI 无法连接

正常停服会删除 `data/runtime/control.sock`。若服务已停止但 socket 残留，确认没有
另一个 XMAgent 进程后删除该单个 socket；不要删除整个 `runtime/`：

```bash
test ! -f data/runtime/control.sock || stat data/runtime/control.sock
```

若服务正在运行而旧 Agent 子进程连接失败，重启/重新触发 Agent 会获得新的 capability。

### 10. 查看诊断信息

```bash
.venv/bin/xmagents doctor
curl -fsS http://127.0.0.1:8765/api/health
```

登录 WebUI 查看“运行日志”和“历史与 Outbox”。日志内容已脱敏；要排查外部网络，
应在代理/Telegram/iLink/模型服务端查看对应请求 ID，而不是打印 Bot Token。

## 十八、真实网络验证清单

自动化测试使用 mock 和 fixture，不能替代以下验证。新服务器部署后建议按顺序执行，
并在完成后删除测试 Agent、测试渠道和测试任务：

1. `doctor` 显示数据库和数据目录权限正常。
2. 用测试管理员浏览器初始化、登录、修改密码、退出，再重新登录。
3. 选择“本机 Claude Code” profile，分别在服务用户已执行 `claude login` 的环境和服务进程继承 `ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_BASE_URL` 的环境中发送一轮文本，确认 `/status` 的 provider/model/effort。
4. 创建普通 Anthropic profile，确认保存的 API 配置可用。
5. 创建 OpenAI-compatible profile，确认 Chat Completions SSE 和多轮历史。
6. 添加一个 Telegram Bot，验证 proxy、私聊首条未绑定提示、管理台绑定后回复、长文本分块和流式编辑。
7. 将测试群组 ID 加入白名单，验证 @Bot、回复 Bot 和未触发的普通群消息。
8. 通过微信二维码登录一个测试账号，验证首条未绑定提示、管理台绑定后的文本和图片/文件、1.5 秒限频及 context token。
9. 启用 `/test`、`/ipv6`，再关闭其中一个 Agent 的插件确认开关生效。
10. 导入带 `documents` 表的测试 SQLite，关联 Agent，验证自动检索和 Anthropic MCP schema/search。
11. 在工作区创建文件，验证 `/sendfile`、`xma-send-file`、路径越界拒绝和 100 MB 限制。
12. 创建 `at`、`every`、`cron` 任务，验证 Telegram 回传、微信 deferred 和取消/重启恢复。
13. 停止服务并恢复一次备份，确认历史、Agent workspace、渠道配置和权限均保留。

完成 smoke test 后，停止测试渠道和服务，按项目交付要求删除 `data/` 中的测试数据库、
工作区、Outbox、知识库副本和 `control.sock`，但不要删除服务器上的正式数据或备份。

## 附录：数据目录结构

```text
data/
├── xmagents.sqlite3       # 配置、历史、消息、记忆、任务、审计、凭据
├── xmagents.sqlite3-wal   # SQLite WAL，运行中可能存在
├── xmagents.sqlite3-shm   # SQLite 共享内存 sidecar，运行中可能存在
├── workspaces/
│   └── <Agent 名称>/
│       ├── CLAUDE.md
│       ├── .claude-config/
│       └── uploads/
├── knowledge/
│   ├── <knowledge-id>.sqlite3
│   └── <knowledge-id>.fts.sqlite3
└── runtime/
    ├── control.sock
    └── knowledge-upload-*.sqlite3  # 导入过程中的临时文件
```

数据库中的 `inbox` 用 `(account_id, external_event_id)` 去重；`outbox` 保存发送状态、
lease、重试次数和结果未知状态。不要直接编辑这些表来“修复”消息，优先通过 WebUI
停止/启动渠道，并保留审计记录。
